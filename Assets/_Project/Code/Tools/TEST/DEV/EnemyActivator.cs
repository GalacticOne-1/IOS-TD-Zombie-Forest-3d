using System;
using Galactic1;
using UnityEngine;

namespace DEV
{
    public class EnemyActivator : MonoBehaviour
    {
        private void Start()
        {
            //GetComponent<EnemyController>().CMD_Activate();
            //GetComponent<EnemyController>().hpRef.CMD_Activate(GetComponent<EnemyController>().param.hp);
        }
    }
}